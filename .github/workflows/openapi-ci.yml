name: OpenAPI CI

permissions:
  contents: read
on:
  pull_request:
    paths:
      - 'docs/openapi/**'
      - '.github/workflows/openapi-ci.yml'
  push:
    paths:
      - 'docs/openapi/**'
      - '.github/workflows/openapi-ci.yml'

jobs:
  lint-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Setup Node.js
        uses: actions/setup-node@1e60f620b9541d910f346b94b622ae0e60eeae34
        with:
          node-version: '20'

      - name: Validate OpenAPI schema structure
        run: npx --yes @apidevtools/swagger-cli@4.0.4 validate docs/openapi/*.yaml

      - name: Lint OpenAPI style and quality
        run: npx --yes @redocly/cli@1.27.1 lint docs/openapi/*.yaml
